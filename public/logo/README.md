# Logo asset (required for deployment)

Place the product logo here so it is included in the build and works on GitHub Pages.

- **Filename:** `bloomrender_bg.png`
- **Path:** `public/logo/bloomrender_bg.png`

This image is used in the header and start screen. Keep the file small (e.g. under 200 KB) so the repo stays light.
